{
  "name": "sofaze-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "start": "yarn workspace sofaze start",
    "build": "yarn workspace sofaze build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "buildDeploy": "yarn build && firebase deploy --only hosting"
  },
  "workspaces": [
    "sofaze"
  ]
}
